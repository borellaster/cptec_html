define(function(require) {
  'use strict';

  var module = require('../module');
  module.factory('AboutFactory', AboutFactory);
  AboutFactory.$inject = [];

  function AboutFactory() {
    var service = {

    };

    return service;

   };
  
});